# Copy to .env and adjust as needed
# PORT is the host port and also the container listening port (see docker-compose.yml)
PORT=4000

# AI provider selection (currently defaults to copilot)
AI_PROVIDER=copilot

# Model name used by Copilot SDK session
COPILOT_MODEL=gpt-5
